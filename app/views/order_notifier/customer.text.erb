Take-Away Order Receipt

Hello <%= @order.user.name -%>,

Your order for take-away food was received.

It will be ready by your pick-up time of <%=@order.pickup_time -%>.

You ordered these <%= @order.order_items.length -%> items:

<%= render @order.order_items -%>
